-- AetherVerified — top-level umbrella module.
-- Round-1 mechanized theorems backing the Rust `aether_verified` crate.

import AetherVerified.Pruning
import AetherVerified.Governor
import AetherVerified.Chebyshev
import AetherVerified.Betti
