url = https://github.com/teerthsharma/Aether-Lang.git
branch = master
reviewed = 131abd0042b53a47b23829b1c1c415d0d66b4bb9

# Baseline set 2026-08-11 to upstream master at the time tracking started.
#
# kernel/aether/Aether-Lang is a FORK, not a vendored snapshot. Measured against
# upstream master 79b210b: 70 of 90 shared files differ (+10343/-7194), 13 files
# exist only here (crates/repl-core, aether-lang/src/{bytecode,typecheck}.rs,
# docs/concepts/epsilon-hollow.md, ...), and 87 exist only upstream (the whole
# aether-core crate, Aether/*.lean). The closest upstream commit by diff size
# (30b5571, 2026-05-02) is still ~6.6k lines away, so no fork point exists to
# merge from.
#
# That divergence is the fork, not an update backlog. Do not try to merge it.
# scripts/aether_upstream.sh reports what moved upstream since `reviewed`;
# port what matters by hand, then `aether_upstream.sh bump`.
