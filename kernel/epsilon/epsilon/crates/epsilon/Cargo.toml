[package]
name = "epsilon"
version = "0.1.0"
edition.workspace = true
authors = ["Teerth Sharma"]
license = "MIT"
description = """
Epsilon: Zero-Shot Context Context Transfer via Topological Surgery.

Pre-Print Reference Implementation (v0.1.0).
Implements O(1) context transfer between AEGIS-powered autonomous agents
via topological surgery on hollow SÂ² manifolds with Betti number Î²â‚‚ = 1.
"""

[dependencies]
libm = { workspace = true }
serde-big-array = { version = "0.5", optional = true }
serde = { workspace = true, optional = true }

# aether-core supplies the exact boundary-matrix reduction this crate's
# beta_2 claim is checked against. Test-only: the published artifact
# gains no runtime dependency, and aether-core does not depend on
# epsilon, so there is no cycle.
[dev-dependencies]
aether-core = { path = "../aether-core" }

[features]
serde = ["dep:serde", "dep:serde-big-array"]
