// ═══════════════════════════════════════════════════════════════════════════════
// AEGIS Seal Loop Demo 🦭
// ═══════════════════════════════════════════════════════════════════════════════
// Demonstrates the unique AEGIS convergence-based seal loop
// Statement terminator: ~ (tilde)
// ═══════════════════════════════════════════════════════════════════════════════

print("╔═══════════════════════════════════════╗")~
print("║     🦭 AEGIS Seal Loop Demo 🦭       ║")~
print("╚═══════════════════════════════════════╝")~

// ═══════════════════════════════════════════════════════════════════════════════
// Example 1: Basic seal loop with counter
// ═══════════════════════════════════════════════════════════════════════════════

print("\n--- Example 1: Basic Counter ---")~

let count = 0~

🦭 until count >= 10 {
    print("  Iteration:", count)~
    count = count + 1~
}

print("Loop sealed after 10 iterations!")~

// ═══════════════════════════════════════════════════════════════════════════════
// Example 2: Seal for loop (range iteration)
// ═══════════════════════════════════════════════════════════════════════════════

print("\n--- Example 2: Range Iteration ---")~

seal for i in 0..5 {
    print("  Value:", i * i)~
}

// ═══════════════════════════════════════════════════════════════════════════════
// Example 3: Manifold convergence (the real power of seal!)
// ═══════════════════════════════════════════════════════════════════════════════

print("\n--- Example 3: Topological Convergence ---")~

// Create data and embed into 3D manifold
let sensor_data = [1.0, 2.1, 1.9, 2.0, 3.5, 4.2, 3.8, 4.0, 5.1, 4.9]~
manifold M = embed(sensor_data, dim=3, tau=2)~

print("Starting regression...")~

// The seal loop runs until the topology stabilizes!
🦭 until convergence(1e-6) {
    regress { model: "polynomial", escalate: true }~
}

print("✓ Manifold sealed! Betti numbers stable.")~

// ═══════════════════════════════════════════════════════════════════════════════
// Example 4: Nested seal loops
// ═══════════════════════════════════════════════════════════════════════════════

print("\n--- Example 4: Nested Loops ---")~

seal for i in 0..3 {
    seal for j in 0..3 {
        print("  [", i, ",", j, "]")~
    }
}

// ═══════════════════════════════════════════════════════════════════════════════
// Example 5: Break and continue in seal loops
// ═══════════════════════════════════════════════════════════════════════════════

print("\n--- Example 5: Break & Continue ---")~

let x = 0~
seal until x >= 20 {
    x = x + 1~
    
    if x % 2 == 0 {
        continue~  // Skip even numbers
    }
    
    if x > 10 {
        break~     // Stop after 10
    }
    
    print("  Odd:", x)~
}

print("\n🦭 All examples complete! The seal has been applied. 🦭")~
