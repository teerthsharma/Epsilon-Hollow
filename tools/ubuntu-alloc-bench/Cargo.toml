[package]
name = "ubuntu-alloc-bench"
version = "0.1.0"
edition = "2024"
rust-version = "1.85"
description = "Ubuntu allocation baseline benchmark for Seal OS proof gates"
publish = false

[lints.rust]
unsafe_code = "warn"

[lints.clippy]
all = "warn"
pedantic = "warn"
